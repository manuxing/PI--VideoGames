import React from "react";

const SearchBar = () => {
    return (
        <form > 
            <input type='text' placeholder={'search'}/>
            <button type="submit"> 
                <img src={{/*nidea*/}} alt='srch'/>
            </button>
        </form>
    );
};

export default SearchBar;